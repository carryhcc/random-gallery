<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.randomGallery.service.mapper.PicServiceMapper">

    <select id="selectPicPage" resultType="com.example.randomGallery.entity.VO.PicVO">
        SELECT
        id,
        pic_url,
        pic_name,
        group_id
        FROM pic_info
        WHERE is_delete = 0
        <if test="qry.groupId != null">
            and group_id = #{qry.groupId}
        </if>
    </select>
    <select id="selectValidPicCount" resultType="java.lang.Long">
        SELECT COUNT(1) FROM pic_info WHERE is_delete = 0
    </select>
    <select id="selectFirstValidIdSince" resultType="java.lang.Long">
        SELECT id FROM pic_info WHERE is_delete = 0 AND id &gt;= #{id} ORDER BY id LIMIT 1
    </select>
</mapper>