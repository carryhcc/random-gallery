export const metadata = {
    title: '图片下载 - 随机图库',
    description: '下载和管理小红书作品',
};

export default function DownloadLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
