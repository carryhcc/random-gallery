export const metadata = {
    title: '随机画廊 - 随机图库',
    description: '瀑布流浏览随机图片',
};

export default function GalleryLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
