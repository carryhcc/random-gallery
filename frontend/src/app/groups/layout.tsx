export const metadata = {
    title: '分组列表 - 随机图库',
    description: '浏览和搜索图片分组',
};

export default function GroupsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
