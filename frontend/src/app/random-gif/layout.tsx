export const metadata = {
    title: '随机动图 - 随机图库',
    description: '浏览随机动图',
};

export default function RandomGifLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
